import RoleSelection from "../components/RoleSelection"; // 👈 adjust the path if you move folders

const Home = () => {
  return (
    <section>
      <RoleSelection />
    </section>
  );
};

export default Home;
